import 'package:flutter/material.dart';

/// Default color of the application
class AppColors {
  /// Default scaffold background color — a soft, warm beige tone for a light
  /// and inviting interface.
  static const scaffoldColorDefault = Color(0xFFFFF5D4);

  /// Warm brown color used for accents, typography, or decorative elements.
  static const warmBrown = Color(0xFFA8744F);

  /// Soft orange color used for highlights, buttons, or interactive elements.
  static const softOrange = Color(0xFFF29C6A);

  /// Secondary dark green color used for backgrounds or contrast elements.
  static const secondaryGreen = Color(0xFF5B734D);

  /// Terracotta red ideal for highlights, subtle alerts, or elements that
  /// convey energy and warmth.
  static const terracottaRed = Color(0xFFC64B3A);

  /// ==========================================
  /// Optimistic gray used in hint text
  static const optimisticGray = Color(0xFF736B66);

  /// Optimistic gray used in hint text
  static const optimisticGray30 = Color(0xFFC9C7C5);

  /// Light yellow used in app
  static const lightYellow = Color(0xffffbd1a);

  /// Orange used in app
  static const empathyOrange = Color(0xffED7E1C);

  /// Used in background color to app screens
  static const mindfulBrown10 = Color(0xffF7F4F2);

  /// Used in background color to app screens
  static const mindfulBrown20 = Color(0xffE8DDD9);

  /// Used in text to app screens
  static const mindfulBrown60 = Color(0xff926247);

  /// Used in text to app screens
  static const mindfulBrown80 = Color(0xff4F3422);

  ///Third color in the light mode
  static const darkBlue = Color.fromRGBO(1, 21, 129, 1);

  /// HexColor('#011581') Color for the success
  static const successGreen = Color.fromRGBO(31, 178, 72, 1);

  ///Color for the error
  static const errorRed = Color.fromRGBO(225, 75, 75, 1);

  /// HexColor('E14B4B') Color for the warning
  static const warningYellow = Color.fromRGBO(247, 155, 34, 1);

  /// HexColor('F79B22')  Primary color in the light mode
  static const mediumBlue = Color.fromRGBO(42, 96, 232, 1);

  /// V_MedBlue
  static const medBlue = Color.fromRGBO(112, 133, 232, 1);

  /// Secondary color in the dark mode
  static const lightBlue = Color.fromRGBO(181, 218, 252, 1);

  /// Background color by data table
  static const background = Color.fromRGBO(228, 230, 238, 1);

  /// Color for a darker white
  static const light = Color.fromRGBO(197, 206, 251, 1);

  /// Background color by data table
  static const lightGrey = Color.fromRGBO(193, 193, 209, 1);

  /// CDCDDD colors grey of placeholder
  static const placeHolderGrey = Color.fromRGBO(205, 205, 221, 1);

  /// Red default of figma 225, 75, 75, 1 delete icon
  static const red = Color.fromRGBO(225, 75, 75, 1);

  /// V_red default figma 252, 231, 235, 1 background delete icon
  static const redLight = Color.fromRGBO(252, 231, 235, 1);

  /// Red grey of colors
  static const redGrey = Color.fromRGBO(191, 148, 148, 1);

  /// Colors default figma orange
  static const orange = Color.fromRGBO(247, 155, 34, 1);

  /// V_orange default
  static const lightOrange = Color.fromRGBO(252, 234, 211, 1);

  /// Background color by data table
  static const selectedLightBlue = Color.fromRGBO(65, 94, 229, 1);

  /// V_Blue Color of not is Colors.white hex DEBEE
  static const subWhite = Color.fromRGBO(235, 238, 253, 1);

  /// White of pastel color
  static const littleWhite = Color.fromRGBO(252, 252, 253, 1.0);

  /// Color gray for use in default
  static const blueGray = Color.fromRGBO(206, 209, 228, 1);

  /// This color is the darkest gray in the system
  static const darkestGray = Color.fromRGBO(80, 79, 89, 1);

  /// V_darkRed default
  static const darkRed = Color.fromRGBO(141, 13, 13, 1);

  /// Color gray for use in default
  static const whiteGrey = Color.fromRGBO(230, 230, 232, 1);

  /// Default dark grey
  static const darkGray = Color.fromRGBO(80, 79, 89, 1);

  /// Color gray for use in default
  static const gray = Color.fromRGBO(182, 181, 204, 1);

  /// Dark blue
  static const grayBlue = Color.fromRGBO(160, 167, 206, 1);

  /// Opacity white
  static const opacityWhite = Color.fromRGBO(254, 254, 255, 1);

  /// Default color green of figma
  static const green = Color.fromRGBO(31, 178, 72, 1);

  /// Color default of progress
  static const colorInProgress = Color.fromRGBO(252, 234, 211, 1);

  /// Color default of finish
  static const colorFinish = Color.fromRGBO(232, 247, 237, 1);

  /// V_red default of figma
  static const lightRed = Color.fromRGBO(252, 231, 235, 1);

  /// FDC500 figma color
  static const goldDark = Color.fromRGBO(253, 197, 0, 1);

  /// FDC500 figma color just light
  static const goldLight = Color.fromRGBO(255, 209, 155, 1);

  /// "Prata" figma color
  static const silverDark = Color.fromRGBO(185, 184, 202, 1);

  /// "Prata" figma color just light
  static const silverLight = Color.fromRGBO(233, 233, 242, 1);

  /// DF953D figma color
  static const bronzeDark = Color.fromRGBO(223, 149, 61, 1);

  /// DF953D figma color just light
  static const bronzeLight = Color.fromRGBO(255, 209, 155, 1);

  /// Color default of created status
  static const createdColor = Colors.transparent;

  /// Color default of progress
  static const colorInProgressFont = Colors.orange;

  /// Color default of finish
  static const colorFinishFont = Colors.green;

  /// V_Yello Colors of yellow default
  static const yellow = Color.fromRGBO(254, 233, 181, 1);

  /// V_Yello Colors of yellow default
  static const darkGreen = Color.fromRGBO(79, 52, 34, 1);

  /// Black default
  static const black = Colors.black;

  /// Color default of scheduled
  static const colorScheduledFont = mediumBlue;

  /// Color default of scheduled
  static const colorScheduled = lightBlue;

  /// Color side bar
  static const lightGreen = Color(0xff8bc386);

  /// Color side bar
  static const mediumGreen = Color(0xff7db079);
}
