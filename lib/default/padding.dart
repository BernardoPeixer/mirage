
import 'package:flutter/material.dart';

/// Class of controller padding panel
class AppPadding {

  /// Padding used to button default
  static const buttonPadding =  EdgeInsets.symmetric(
    horizontal: 24,
    vertical: 16,
  );

  /// Padding horizontal default in all components screen
  static const itemHorizontal = EdgeInsets.symmetric(horizontal: 4.0);

  /// Padding large horizontal default in components screen
  static const itemLargeHorizontal = EdgeInsets.symmetric(horizontal: 8.0);

  /// Padding vertical default in all components screen
  static const itemVertical = EdgeInsets.symmetric(vertical: 4.0);

  /// Padding large vertical default in components screen
  static const itemLargeVertical = EdgeInsets.symmetric(vertical: 8.0);

  /// Padding default in all screen
  static const screen = EdgeInsets.symmetric(horizontal: 15);

  /// Padding horizontal default in all components screen
  static const body = EdgeInsets.only(
    top: 4.0,
    left: 12.0,
    right: 16.0,
    bottom: 12.0,
  );

  static const dataTable = EdgeInsets.symmetric(horizontal: 16, vertical: 10);

  /// TextForm padding use in default in new Constructor
  /// [TextFormBoxDefault.padding]
  static const textForm = EdgeInsets.symmetric(
    vertical: 8.0,
    horizontal: 10.0,
  );

  /// TextForm padding use in default in new Constructor
  /// [TextFormBoxDefault.padding]
  static const textFormLong = EdgeInsets.symmetric(
    vertical: 8.0,
  );

  /// TextForm padding use in default in new Constructor
  /// [TextFormBoxDefault.padding]
  static const textFormSmall = EdgeInsets.symmetric(
    vertical: 6.0,
    horizontal: 2.0,
  );

  /// Padding of contains in list data table
  static const actionsList = EdgeInsets.only(right: 6);

  /// Padding of [typeFilter] in screen
  static const typeFilter = EdgeInsets.only(
    right: 4.0,
    bottom: 8.0,
  );

  /// Padding of [actionsForms] in screen
  static const actionsForms = EdgeInsets.only(
    left: 4.0,
    right: 6.0,
  );

  /// Padding of contains in actions form
  static const actionsForm = EdgeInsets.only(right: 8);

  /// Padding of [buttonForm] in screen
  static const buttonForm = EdgeInsets.symmetric(
    vertical: 12.0,
    horizontal: 24.0,
  );

  /// Padding of [buttonList] in screen
  static const buttonList = EdgeInsets.symmetric(
    vertical: 4.0,
    horizontal: 6.0,
  );

  /// Padding horizontal default in all components screen
  static const buttonModal = EdgeInsets.symmetric(
    vertical: 12.0,
    horizontal: 24.0,
  );

  /// Padding horizontal default in all components screen
  static const buttonHeader = EdgeInsets.symmetric(
    vertical: 8,
    horizontal: 16,
  );

  /// Padding horizontal default in all components screen
  static const buttonModalLittle = EdgeInsets.symmetric(
    vertical: 8.0,
    horizontal: 8.0,
  );

  /// Padding of [buttonFormMedium] in screen
  static const buttonFormMedium = EdgeInsets.symmetric(
    vertical: 10.0,
    horizontal: 8.0,
  );

  /// Padding of [buttonFormSmall] in screen
  static const buttonFormSmall = EdgeInsets.symmetric(
    vertical: 8.0,
    horizontal: 8.0,
  );

  /// Padding of [buttonFormField] in screen
  static const buttonFormField = EdgeInsets.symmetric(
    vertical: 6.0,
    horizontal: 10.0,
  );

  /// Padding of contains in card register default
  static const card = EdgeInsets.all(16.0);

  /// Padding use in TitleDefault
  static const title = EdgeInsets.only(
    top: 20.0,
    left: 10.0,
    bottom: 8.0,
  );

  /// Padding use in header
  static const header = EdgeInsets.only(bottom: 4);

  /// Padding use in buttonHeaderStrong
  static const buttonHeaderStrong = EdgeInsets.symmetric(
    horizontal: 16,
    vertical: 10.5,
  );

  /// Padding use for tiny body
  static const tinyBody = EdgeInsets.all(8);

  /// Padding use in button forms
  static const formButton = EdgeInsets.symmetric(
    horizontal: 8,
    vertical: 4,
  );
}
